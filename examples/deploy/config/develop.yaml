# Example Application manifesto (copy to deploy/config/develop.yaml).
# Pipeline selects chart from kind (asa-application). Ports are platform-owned (8080).

kind: Application

workload:
  type: web

legacyDns: false

# Required for web/grpc: probes: false OR explicit per-type probes (no global path).
probes: false

autoscaling:
  minReplicas: 1
  maxReplicas: 3
  cpu:
    target: 70

# probes:
#   readiness:
#     path: /health/ready
#   startup:
#     path: /health/startup

# serviceAccount:
#   annotations:
#     eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT:role/sample-api-develop

# workloadIdentity:
#   gcp:
#     audience: //iam.googleapis.com/projects/PROJECT_NUMBER/locations/global/workloadIdentityPools/POOL/providers/PROVIDER
#     serviceAccountEmail: app-sa@my-gcp-project.iam.gserviceaccount.com
#     projectId: my-gcp-project

# persistence:
#   mountPath: /data
#   size: 1Gi
# # requires autoscaling: false and replicaCount: 1

# Use __ for .NET hierarchical config (not ':' as ConfigMap keys).
config:
  API_URL: https://api.dev.example
  FEATURE_FLAG: "true"
  # ConnectionStrings__Default: "..."

# externalSecret:
#   secretStoreRef:
#     kind: ClusterSecretStore
#     name: aws-secretsmanager
#   data:
#     - secretKey: ConnectionStrings__Default
#       remoteRef:
#         key: asa/sample-api/develop/connection-string

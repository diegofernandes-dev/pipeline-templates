apiVersion: v2
name: app
description: Platform Helm chart for .NET APIs on EKS (frozen functional scope — see README Freeze)
type: application
# 1.0.0 = Helm functional freeze for .NET/EKS API scope.
version: 1.0.0
appVersion: "1.0.0"
